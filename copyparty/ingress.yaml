apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: copyparty
  namespace: copyparty
spec:
  defaultBackend:
    service:
      name: copyparty
      port:
        number: 80
  ingressClassName: tailscale
  tls:
    - hosts:
        - copy